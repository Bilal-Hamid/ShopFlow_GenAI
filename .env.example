# Used by docker-compose.yml to configure the db/redis/api services.
# Copy to .env and adjust as needed: cp .env.example .env

POSTGRES_USER=shopflow
POSTGRES_PASSWORD=shopflow
POSTGRES_DB=shopflow
POSTGRES_PORT=5432

REDIS_PORT=6379

APP_ENV=local
APP_PORT=8000

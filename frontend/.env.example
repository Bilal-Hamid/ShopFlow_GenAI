# Base URL of the ShopFlow backend API (Domain 1). Backend runs on :8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Toggle MSW mocking. Leave empty to use the real backend. Set to "enabled" to mock.
NEXT_PUBLIC_API_MOCKING=
